# 隐私说明

最后更新：2026-08-26

种马搜索器安装助手的设计目标是完成本机扩展文件的安装与更新，不收集用户个人信息。

## 不读取、不收集的内容

助手不会读取、收集、上传或保存以下内容：

- 姓名、邮箱、电话号码或账号资料；
- Windows 用户名或设备唯一标识；
- 浏览器 Cookie、密码、浏览历史、书签或浏览器个人资料；
- B 站或其他网站的登录状态；
- 扩展中的筛选偏好和使用记录；
- 遥测、崩溃报告或使用统计。

## 网络访问

助手只为检查和下载扩展版本访问：

- `api.github.com/repos/yyahz/umamusume-seed-optimizer/releases/latest`
- 该公开仓库 GitHub Release 返回的 `github.com` HTTPS 下载地址

GitHub 可能依据其自身政策记录常规网络信息，例如 IP 地址和请求时间。助手不会在请求中加入用户姓名、邮箱、Cookie 或设备标识。

点击“打开吗哩吗哩”只会命令用户选择的浏览器打开对应公开网页；网页交互由浏览器处理，助手不会读取网页内容或登录状态。

## 本地文件

助手只在 `%LOCALAPPDATA%\UmaSeedSearcher` 下保存扩展文件、更新备份和安装过程中的临时文件。下载包在操作结束后删除。失败时会尽力恢复原扩展目录。早期测试目录仅在用户明确同意后迁移，不会静默覆盖新目录。

## 权限

助手以当前用户权限运行，不申请管理员权限，不修改注册表，不读写浏览器个人资料目录，也不会强制关闭浏览器。

Compact 与 Full 两种下载只在运行库打包方式上不同，网络访问、本地目录和隐私行为完全一致。

## 变更

如果未来加入任何数据收集功能，必须先更新本说明，并在界面中明确提示；默认不会静默启用。
