# 安全说明

## 下载与安装保护

安装助手只信任公开仓库 `yyahz/umamusume-seed-optimizer` 的最新 GitHub Release，并执行以下检查：

1. Release 标签和扩展 ZIP 文件名中的版本必须一致；
2. 下载地址必须是 `github.com` 的 HTTPS 地址；
3. 文件大小必须与 GitHub Release 元数据一致且不超过限制；
4. 下载内容必须匹配 GitHub Release 提供的 SHA256 摘要；
5. ZIP 必须通过路径穿越、重复路径、文件数量和解压体积检查；
6. 只允许扩展需要的 JSON、JavaScript、PNG、Markdown 和许可证文件；
7. `manifest.json` 必须位于根目录，使用 Manifest V3，名称和版本必须匹配。

更新采用“新版本先完整解压并验证，再备份旧目录并替换”的方式。替换失败时会恢复旧目录。助手拒绝在安装根目录为文件系统链接或联接点时继续操作。

## 已知限制

- 测试版可执行程序未进行商业代码签名，Windows SmartScreen 可能提示未知发布者；
- SHA256 证明下载内容与 GitHub Release 一致，但仓库账号安全仍依赖 GitHub 账号自身的保护；
- 非商店加载的扩展必须由用户开启开发者模式，浏览器可能显示相关提醒；
- 浏览器运行时更新文件后，通常需要用户在扩展管理页点击“重新加载”。

## 报告问题

请在私有仓库创建 Issue，提供复现步骤、助手版本和不含隐私信息的错误提示。不要上传 Cookie、账号信息、浏览器资料文件或包含个人路径的完整日志。
